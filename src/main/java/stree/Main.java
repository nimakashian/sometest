package stree;

import java.util.Hashtable;

public class Main {
    /*final static STree events = new STree();
    static Hashtable<Integer, Integer> hashtable = new Hashtable();


    public static void main(String[] args) {
        hashtable.put(2,3);
        init();
    }

    public static void init() {
        for (int i = 0; i < 30; i++)
            for (int j = 0; j < 30; j++)
                events.append("wow", i + "@" + i);

        System.out.println("hi");


        for (int k = 0; k < 3000000; k = k + 30000) {
            long dur = 0;
            long t1 = 0, t2 = 0;
            for (int i = 0; i < 30000; i++) {
                t1 = System.currentTimeMillis();
                events.append("wow", 16 + "@" + 16);
                t2 = System.currentTimeMillis();
                dur = dur + (t2 - t1);
                events.remove(16, 0);
            }
            System.out.println(k + ":" + (float) (dur) / 30000);
        }

        System.out.println(events.getNodesSize());
        System.out.println(events.getSize());
        System.out.println(events.getStructure());

    }*/
}